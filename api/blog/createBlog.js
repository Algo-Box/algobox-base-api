const { writeResponse } = require('../../util/util')

const createBlog = (req, res) => {
  // TODO
  writeResponse({
    "haha": "functionNotImplemented"
  }, null, res);
}

module.exports = createBlog;